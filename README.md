A simple study of how to process images using Rust, utilising the ```image``` crate

to open: 

clone the code > cargo build > cargo run

What's currently there:

opening file > saving file > simple hue manipulation
